(function () {
  'use strict';

  /*
   * Image loading effects were intentionally retired. Layout stability now
   * comes from intrinsic width/height attributes, aspect-ratio containers,
   * lazy loading, and the static CSS placeholder in assets/input.css.
   */
})();
